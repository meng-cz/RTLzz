# s3statementize fixtures

This directory is reserved for source-level fixtures for the statementize stage.
The current unit test constructs focused ASTs directly so it can assert the
exact statementized debug print independent of libclang cursor encoding.
